<%= render 'enrollments/enrollment', enrollment: @enrollment %>
<p></p>
<div class="btn-group" role="group">
  <%= button_to 'Back', enrollments_path, method: :get, :class => "btn shiny btn-yellow" %>
</div>
